---
name: TfSec

# yamllint disable-line rule:truthy
on:
  pull_request:
  schedule:
    # runs mondays at 3:47 UTC refreshing the vulnerabilities
    - cron: "47 3 * * 1"

jobs:
  default:
    # yamllint disable-line rule:line-length
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/terraform_module_terraform_tfsec_callable.yml@5c9ed2712cf4a213138df8ab92f4e1384712784a # 1.25.8
    secrets: inherit
